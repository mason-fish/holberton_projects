#include <stdlib.h>

char **string_split(char *s) {
  char **cPPtr;
  
  

  return cPPtr;
}

int find_strings (char s*) {
  int i;
  
  for (i = 0; *s == '\0'; i++) {
    while (*s != ' ' && *s != '\t' && *s != '\r' && *s != '\n') {
      
    }
  }
  
  return i;
}
